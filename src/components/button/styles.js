import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    backgroundColor: '#f00',
    width: '100%',
    paddingVertical: 10,
    marginBottom: 10
  },
  text: {
    color: '#fff',
    fontWeight: 'bold',
    textAlign: 'center'
  }
});

export default styles;
