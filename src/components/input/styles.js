import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    marginVertical: 10,
    borderRadius: 5,
    borderColor: '#444',
    padding: 10,
    color: '#444'
  },
  label: {
    color: '#444',
    fontSize: 20,
    fontWeight: 'bold'
  },
  invalid: {
    color: '#f00',
    borderColor: '#f00'
  }
});

export default styles;
