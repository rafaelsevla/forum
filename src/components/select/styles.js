import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    marginVertical: 10,
    borderRadius: 5,
    borderColor: '#444',
    padding: 10
  },
  label: {
    color: '#444',
    fontSize: 20,
    fontWeight: 'bold'
  }
});

export default styles;
